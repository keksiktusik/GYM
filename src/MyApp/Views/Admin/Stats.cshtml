@{
    ViewData["Title"] = "Statystyki systemu";
    var roles = ViewBag.RoleStats as Dictionary<string, int>;
}

<div class="container mt-5">
    <h2 class="text-center mb-4">📊 Statystyki systemu</h2>

    <div class="card shadow-sm p-4">
        <p><strong>👥 Liczba wszystkich użytkowników:</strong> @ViewBag.Total</p>
        <p><strong>📧 Potwierdzone konta:</strong> @ViewBag.Confirmed</p>
        <p><strong>🚫 Zablokowani użytkownicy:</strong> @ViewBag.Locked</p>

        <hr />
        <h5>📂 Użytkownicy w rolach:</h5>
        <ul>
            @foreach (var item in roles)
            {
                <li>@item.Key: <strong>@item.Value</strong></li>
            }
        </ul>

        <div class="mt-3">
            <a asp-action="Users" class="btn btn-secondary">⬅ Powrót do listy</a>
        </div>
    </div>
</div>
